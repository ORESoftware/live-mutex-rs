# Elixir client seed - `dd-rust-network-mutex`

Dependency-free Elixir protocol mirror for the broker wire format. It uses
atoms for request/response discriminators and small JSONL request builders.

## Run

```bash
mix test
```

The test is offline: no broker is required.

