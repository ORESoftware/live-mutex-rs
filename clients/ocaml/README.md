# OCaml client seed - `dd-rust-network-mutex`

Dependency-free OCaml protocol mirror for the broker wire format. It provides
variant types for request/response discriminators and JSONL request builders.

## Run

```bash
make test
```

The test is offline: no broker is required.

